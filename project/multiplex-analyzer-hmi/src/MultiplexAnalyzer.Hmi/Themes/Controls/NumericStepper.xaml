<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:MultiplexAnalyzer.Hmi.Controls">

    <Style x:Key="Stepper.RepeatButton" TargetType="RepeatButton">
        <Setter Property="Width" Value="{StaticResource TouchTarget.Min}"/>
        <Setter Property="Delay" Value="400"/>
        <Setter Property="Interval" Value="60"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border x:Name="Root" Background="Transparent">
                        <TextBlock x:Name="Glyph" Text="{TemplateBinding Content}"
                                   FontFamily="{StaticResource Font.Family}" FontSize="22"
                                   FontWeight="SemiBold"
                                   Foreground="{TemplateBinding Foreground}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.Surface.Raised}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.Surface.Sunken}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Glyph" Property="Foreground" Value="{DynamicResource Brush.Text.Disabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:NumericStepper}">
        <Setter Property="Height" Value="{StaticResource TouchTarget.Min}"/>
        <Setter Property="MinWidth" Value="200"/>
        <Setter Property="Background" Value="{DynamicResource Brush.Surface.Base}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Strong}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:NumericStepper}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource Radius.M}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <RepeatButton x:Name="PART_Decrease" Grid.Column="0"
                                          Content="-" Style="{StaticResource Stepper.RepeatButton}"/>

                            <StackPanel Grid.Column="1" Orientation="Horizontal"
                                        HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock FontFamily="{StaticResource Font.Family}" FontSize="18"
                                           FontWeight="SemiBold"
                                           Foreground="{TemplateBinding Foreground}"
                                           Text="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, StringFormat={}{0:0.#}}"/>
                                <TextBlock Text="{Binding Unit, RelativeSource={RelativeSource TemplatedParent}}"
                                           FontFamily="{StaticResource Font.Family}" FontSize="14"
                                           Margin="4,0,0,0" VerticalAlignment="Center"
                                           Foreground="{DynamicResource Brush.Text.Secondary}"/>
                            </StackPanel>

                            <RepeatButton x:Name="PART_Increase" Grid.Column="2"
                                          Content="+" Style="{StaticResource Stepper.RepeatButton}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
