<Window x:Class="UnitTestShowcase.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UnitTestShowcase.App.ViewModels"
        mc:Ignorable="d"
        Title="UnitTest Showcase - Product Manager"
        Height="600" Width="900"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Unit Test Showcase - Product Manager"
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,5,0" Padding="4"/>
            <Button Grid.Column="1" Content="Search" Command="{Binding SearchCommand}"
                    Padding="10,4" Margin="0,0,5,0"/>
            <Button Grid.Column="2" Content="Load All" Command="{Binding LoadProductsCommand}"
                    Padding="10,4"/>
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Products}"
                  SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="80"/>
                <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="120" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="3" Header="Add New Product" Margin="0,0,0,8" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Grid.Column="1" Text="{Binding NewProductName, UpdateSourceTrigger=PropertyChanged}"
                         Padding="4" Margin="0,0,8,0"/>
                <TextBlock Grid.Column="2" Text="Price:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Grid.Column="3" Text="{Binding NewProductPrice, UpdateSourceTrigger=PropertyChanged}"
                         Padding="4" Margin="0,0,8,0"/>
                <TextBlock Grid.Column="4" Text="Stock:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Grid.Column="5" Text="{Binding NewProductStock, UpdateSourceTrigger=PropertyChanged}"
                         Padding="4" Margin="0,0,8,0"/>
                <Button Grid.Column="6" Content="Add" Command="{Binding AddProductCommand}"
                        Padding="12,4"/>
            </Grid>
        </GroupBox>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center"
                       Foreground="DarkBlue"/>
            <Button Grid.Column="1" Content="Update Selected" Command="{Binding UpdateProductCommand}"
                    Padding="10,4" Margin="0,0,5,0"/>
            <Button Grid.Column="2" Content="Delete Selected" Command="{Binding DeleteProductCommand}"
                    Padding="10,4" Foreground="Red"/>
        </Grid>
    </Grid>
</Window>
