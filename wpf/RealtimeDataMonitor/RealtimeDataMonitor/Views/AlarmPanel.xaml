<UserControl x:Class="RealtimeDataMonitor.Views.AlarmPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:RealtimeDataMonitor.Models">
    <Grid Background="#1A1A24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#22222E" Padding="10,6">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="ALARMS" FontWeight="Bold" Foreground="#E63946"
                           FontSize="14" VerticalAlignment="Center"/>
                <Border Background="#E63946" CornerRadius="10" Margin="8,0,0,0"
                        Padding="6,2"
                        Visibility="{Binding Alarm.CriticalAlarmCount, Converter={StaticResource CountToVisibilityConverter}}">
                    <TextBlock Text="{Binding Alarm.CriticalAlarmCount}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                </Border>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8,6" >
            <Button Content="Acknowledge All"
                    Command="{Binding Alarm.AcknowledgeAllCommand}"
                    Style="{StaticResource ModernButton}"
                    Margin="0,0,6,0"/>
            <Button Content="Clear All"
                    Command="{Binding Alarm.ClearAllCommand}"
                    Style="{StaticResource DangerButton}"/>
        </StackPanel>

        <ListView Grid.Row="2"
                  ItemsSource="{Binding Alarm.Alarms}"
                  Background="Transparent"
                  BorderThickness="0"
                  Foreground="White">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="4,2"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Severity}" Value="{x:Static models:AlarmSeverity.Critical}">
                            <Setter Property="Background" Value="#3A1A1A"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Time" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Timestamp, StringFormat=HH:mm:ss}" FontSize="11"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Severity" Width="70">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Severity}" FontSize="11"
                                           Foreground="{Binding Severity, Converter={StaticResource SeverityToColorConverter}}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Description" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Description}" FontSize="11" TextWrapping="Wrap"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
